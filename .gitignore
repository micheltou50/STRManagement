.DS_Store
.claude/
admin-page-mockup.html

# Local Netlify folder
.netlify
node_modules

# Capacitor web build output (assembled by scripts/build-www.mjs; ios/ is committed)
/www

# Supabase — CLI-managed bits stay local, but migrations/ IS committed
# (the repo's schema source of truth; see supabase/migrations/README.md).
/supabase/functions
/supabase/.branches
/supabase/.temp
/supabase/config.toml

Netlify/.DS_Store
